# 에이전트용 설치 지시

이 문서는 사람이 아니라 **코딩 에이전트**가 읽고 실행하는 설치 절차다.

## 절차

1. 저장소 루트에서 `./install.sh`를 실행한다. 설치기가 의존성 확인, `config.sh` 생성,
   프로필 생성, launchd 등록, 첫 리포트 발행까지 전부 한다. **직접 단계를 재현하지 말고 이 스크립트를 써라.**
2. 실패하면 아래 "막히는 지점"을 보고 해당 항목만 처리한 뒤 `./install.sh`를 다시 실행한다.
   설치기는 몇 번을 실행해도 안전하다(이미 있는 설정·프로필은 건드리지 않는다).
3. 성공하면 생성된 `PROFILE.md`를 사용자와 함께 검토한다. 이게 추천 품질을 결정하므로 가장 중요한 단계다.

## 막히는 지점

**`claude CLI 인증이 안 돼 있습니다`**
사용자에게 터미널에서 `claude`를 직접 실행해 로그인하도록 요청한다. 에이전트가 대신 로그인할 수 없다.

**`사용 기록이 없습니다`**
이 도구는 사용자의 Claude Code / Codex 기록에서 프로필을 만든다. 기록이 없으면 설치해도 쓸모가 없다.
며칠 사용한 뒤 다시 설치하라고 안내하고 중단한다.

**아침에 조용히 실행되지 않음**
macOS의 백그라운드 항목 승인 문제다. 시스템 설정 → 일반 → 로그인 항목 및 확장 프로그램 →
"백그라운드에서 허용"에서 `zsh` 항목을 켜라고 안내한다. 에이전트가 이 토글을 대신 켤 수 없다.
`launchctl kickstart`로 수동 실행하면 이 검문을 우회하므로, "수동은 되는데 예약은 안 되는" 증상이면 이걸 의심한다.

**cron으로 바꾸지 말 것**
cron은 GUI 세션 밖에서 돌아 로그인 키체인에 접근하지 못하고, `claude` CLI가 반드시
`OAuth session expired`로 실패한다. 실측으로 확인된 사항이다. 반드시 launchd를 쓴다.

## 설치 후 사용자와 함께 볼 것

`PROFILE.md`를 열어 다음을 확인한다.

- **"지금 집중 중인 것"이 실제와 맞는가.** 틀렸다면 대개 원인은 하나다 — Codex Desktop에서
  서브에이전트로 주로 작업한 프로젝트가 과소평가된 경우. 이 도구는 서브에이전트 세션도 세도록
  되어 있지만, 그래도 빠졌다면 `PROFILE.md`를 직접 고치면 된다(다음 재생성 때 덮어써지므로,
  계속 틀리면 사용자에게 알려달라고 한다).
- **프로젝트마다 붙은 `매칭:` 키워드 줄.** 이게 1,100여 건의 HN 목록에서 관련 글을 낚는 고리다.
  사용자가 보기에 빠진 주제어가 있으면 여기에 추가하라고 권한다. 추천 품질을 올리는 가장 빠른 방법이다.
- **회사 트랙을 쓰는 경우** `config.sh`의 `COMPANY_NAME`, `COMPANY_DOMAIN`, `COMPANY_KEYWORDS`가
  사용자의 실제 업무 영역과 맞는지 확인한다. 기본값은 헬스케어 기준이다.

마지막으로 오늘 리포트(`report-<오늘날짜>.html`)를 열어 함께 훑고, 빗나간 항목이 있으면
그 원인이 프로필인지 키워드인지 짚어준다.

## 하지 말 것

- `PROFILE.md`, `COMPANY.md`, `report-*`, `digest-*`, `logs/`를 커밋하지 마라.
  진행 중인 사업과 고객사 정보가 들어 있다. `.gitignore`에 이미 등록돼 있으니 그 파일을 수정하지 마라.
- `config.sh`의 `DAILY_MODEL`을 비우지 마라. 비우면 대화창 기본 모델을 물려받아 사용량이 몇 배가 된다.
- 리포트 생성 단계에 WebFetch 같은 도구를 다시 붙이지 마라. 도구를 쓰면 매 턴 컨텍스트가
  누적돼 비용이 25배가 된다. 웹 수집은 `fetch_pages.py`가 LLM 없이 처리하도록 되어 있다.
