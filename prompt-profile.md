아래는 한 개발자의 활동 다이제스트다. 코딩 에이전트(Claude Code / Codex) 사용 기록에
디스크 git·GitHub 이벤트·열려 있는 편집기 창이 붙어 있다.
이걸 근거로 이 사람의 프로필을 작성하라. 형식은 markdown, 출력은 프로필 본문만 (인사말·설명 없이).

## 신호를 읽는 법

에이전트 프롬프트는 **무엇에 관심이 있나**를, 나머지 셋은 **실제로 무엇을 했나**를 말한다. 어긋나면 후자가 이긴다.

- **디스크 git** — 커밋이 실제로 나간 곳. 프롬프트는 많은데 내 커밋이 0건이면 그건 '작업'이 아니라 '조사·상담'이었다고 보고, 집중 중인 것에 넣지 마라. 브랜치명은 지금 붙어 있는 주제를 직접 말해준다(`feat/time-travel-emulator`) — 매칭 키워드를 뽑을 때 특히 유용하다.
- **GitHub 이벤트** — 로컬에 안 남는 것(내가 올린 PR·리뷰·코멘트). PR·이슈가 많고 푸시가 적은 레포는 코드보다 조율에 시간을 쓰는 곳이다. 여기 있는 이름은 로컬 폴더명과 다를 수 있으니 같은 프로젝트로 묶어 판단하라.
- **열려 있는 VS Code 창** — '지금 이 순간' 신호. 순위는 없고 열림/닫힘뿐이지만, `마지막 활성 창`은 가장 강한 단일 근거다. 반대로 활동량이 있어도 창이 닫혀 있으면 방치 쪽으로 기울여라.

**신호가 없는 것과 0인 것은 다르다.** git 목록에 아예 없는 프로젝트는 git 레포가 아니거나 다른 곳에 있다는 뜻이고, VS Code에 없으면 다른 편집기를 쓴다는 뜻일 수 있다. 강등은 신호가 **있으면서 0일 때만** 하라. 없는 신호를 근거로 프로젝트를 내리지 마라.

## 빠뜨리지 마라

다이제스트에 최근 2주 활동이 있는 프로젝트는 **하나도 빠짐없이** 1번이나 2번에 넣어라. 분류가 애매하면 애매하다고 쓰고 넣어라 — 빼지 마라. 어느 쪽인지 못 정하겠으면 2번(방치)에 넣고 판단을 미루면 된다.
누락은 조용히 품질을 깎는다. 프로필에 없는 프로젝트는 뉴스 선별에서 **아예 후보가 되지 못한다**. `매칭:` 줄이 하나 빠지면 그 주제의 글은 1,100건 목록에서 영구히 낚이지 않는다.
같은 프로젝트가 폴더명·GitHub 레포명으로 다르게 나타나면 하나로 묶고 두 이름을 다 적어라.

포함할 섹션:
1. **지금 집중 중인 것** — 최근 2주 활동이 몰린 프로젝트, 무엇을 왜 하는지
2. **방치/중단된 것** — 활동량은 있었는데 오래 멈춘 프로젝트, 재개 가치 판단
3. **관심 기술과 일하는 방식** — 프롬프트에서 드러나는 기술 스택, 반복되는 관심사, 특징적인 작업 패턴
4. **사업화·사이드 프로젝트 관심사** — 개인 프로젝트에서 드러나는 방향

각 항목은 다이제스트의 실제 근거(프로젝트명, 날짜, 프롬프트 요지)를 들어라. 추측은 '추정'이라고 표시.

1번과 2번에서 프로젝트를 하나 쓸 때마다 그 아래에 반드시 다음 한 줄을 붙여라:
  `매칭: 키워드1, 키워드2, ...`
이 줄은 '기술 뉴스 목록에서 이 프로젝트와 관련된 글을 골라낼 때 쓸 검색어'다.
그 프로젝트가 쓰는 기술·다루는 도메인·풀려는 문제를 6~10개의 구체적 단어로 적어라.
한국어와 영어를 섞어서 쓰고(영문 기술 용어가 실제 매칭에 쓰인다), '개발·AI' 같은 너무 넓은 말은 쓰지 마라.
예: `매칭: fountain pen, handwriting, typography, haptics, motion design, journaling, note-taking app, e-ink`
