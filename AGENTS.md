# hn-researcher — 에이전트 지침

설계와 함정 목록은 `docs/ARCHITECTURE.md`, 범위와 계획은 `docs/ROADMAP.md`. 여기 옮겨 적지 말 것.
`docs/ROADMAP.md`의 `다음` 항목을 끝냈으면 지우지 말고 `한 것`으로 내릴 것 — 왜 그 선에서 멈췄는지는 커밋 로그에 안 남는다.

## 실행

```sh
./daily.sh                    # 아침 발행 전체 (launchd가 부르는 것)
./run.sh [YYYY-MM-DD]         # 리포트만 (인자 없으면 지난 24시간)
python3 -m unittest discover -s tests

python3 knowledge.py --pending      # 재심 대상(안 쓰인 후보) 확인
python3 knowledge.py --ingest DATE  # 후보를 누적 번들에 반영 (run.sh가 자동으로 부른다)
python3 sources.py --digest        # HN 외 소스만 확인
python3 sources.py --releases "$RELEASE_REPOS"   # 릴리스만 확인
```

## 기본과 다른 제약

- **파이썬 표준 라이브러리만.** 의존성을 추가하지 말 것. 새 기능이 패키지를 요구하면 먼저 물어볼 것.
- **`claude -p`에는 항상 `--model`을 명시.** 빼면 대화용 기본 모델을 물려받아 배치가 최상위 티어로 돌고 사용량이 몇 배가 된다. 일일은 sonnet, 주간은 상위 모델.
- **`--disallowedTools`는 쉼표로 구분하고 프롬프트를 이 옵션보다 앞에 둘 것.** 가변 인자라 뒤따르는 인자를 도구 이름으로 삼킨다. `--allowedTools ""`는 무시된다.
- **cron을 쓰지 말 것.** GUI 세션 밖이라 키체인에 못 닿아 인증이 항상 실패한다. launchd만 쓴다.
- **`[ 조건 ] && 명령`을 스크립트 마지막 줄에 두지 말 것.** 조건이 거짓이면 종료 코드가 1이 된다.

## 행동은 프롬프트에 있다

선별 기준·리포트 구조·통과 조건은 전부 `prompts/`에 있고 파이썬은 배관이다.
품질 문제는 코드보다 프롬프트를 먼저 고칠 것.

## 손으로 쓰는 파일을 코드가 쓰지 말 것

`INTERESTS.md`는 사용자가 손으로 쓴다. 어떤 스크립트도 이 파일에 쓰면 안 된다 — `PROFILE.md`가 재생성 때 덮어써지는 문제를 피하려고 분리한 것이다. 읽기만 한다.

## 누적 번들에 화면을 붙이지 말 것

`out/knowledge/`는 기계 쪽에만 있다. 보류 항목을 리포트나 HTML에 목록으로 노출하면 미독 백로그가 되고, 그건 `docs/ROADMAP.md`가 이탈 1번 원인으로 적어 금지한 것이다. 보류는 오늘 한 장 안에서 오늘 것과 동등하게 경쟁해서만 다시 나온다.

## 커밋 금지

`PROFILE.md`, `config.sh`, `out/`, `logs/` — 진행 중인 사업과 고객사 정보가 들어간다. `.gitignore`에 있으니 그 파일을 수정하지 말 것.
공개 저장소이므로 커밋 전에 고객사·사내 용어를 훑을 것.

## 리뷰

Codex가 검토자다(`/codex:review`, `/codex:adversarial-review`). **일부러 다른 공급자**를 쓴다 —
같은 계열로 검증하면 훈련 데이터와 선호를 공유해 독립성이 무너진다.
자동 Stop 훅 게이트는 켜지 않는다. 수동 리뷰가 부담이 될 때 다시 판단한다.

## 테스트

`tests/`의 각 테스트는 실제로 겪은 버그 하나에 대응한다. 새 버그를 고칠 때 테스트를 하나 남길 것.
LLM 호출은 테스트하지 않는다 — 결정론적인 부분(집계·중복제거·렌더)만 검증한다.
