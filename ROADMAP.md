# 범위와 계획

## 이 도구가 하는 일

1. 내가 지금 붙어 있는 일을 로컬·GitHub에서 **자동으로** 알아낸다
2. 그에 맞는 양질의 정보를 골라낸다
3. 읽고 파악하고 바로 쓰게 정리한다

이 셋에만 에너지를 쓴다. 어떤 기능을 넣을지 판단이 서지 않을 때는 "이게 셋 중 어디에 속하나"를 먼저 묻는다. 어디에도 안 들어가면 안 만든다.

## 정해진 것

- **LLM 호출은 기본이 구독이다.** `claude -p` CLI로 돈다. 구독료를 이미 내고 있으므로 Messages API로 옮기는 것은 같은 일에 두 번 지불하는 것이다. API는 원하는 사람만 켜는 옵션으로 남기고, 기본 경로로 삼지 않는다.
- **GitHub 신호는 내 활동만**, `gh` CLI 경유(파이썬 의존성 0). private·조직 레포까지 보이고 rate limit은 시간당 5,000 중 1~3콜만 쓴다.

## 하지 않는 일

의도적으로 비워 둔 자리다. 나중에 흔들리지 않기 위해 이유를 적어 둔다.

- **뉴스 아카이브·검색** — 어제 것은 사라진다. 백로그가 쌓이면 미독 죄책감이 생기고, 그게 이 카테고리 이탈의 1번 원인이다.
- **팀 공유·협업** — 1인용 로컬 도구다. 공유가 붙으면 프로필의 개인 데이터를 어디까지 노출할지가 문제가 되고, 그 문제는 이 도구가 풀 문제가 아니다.
- **회사 사업개발** — 규제·수익모델·구매자 예산을 따지는 사업 컨설팅은 세 축 어디에도 안 들어간다. 근거의 8할이 외부 지식이라 이 도구가 특별히 잘할 이유도 없다. 주간 리포트는 **내가 개인으로 작게라도 돈을 벌 수 있는 것**만 본다.
- **챗봇·대화 인터페이스** — 아침에 한 장 읽는 것이 전부여야 한다. 물어보게 만들면 읽는 시간이 늘어난다.
- **이메일 발송·서버** — 로컬에서 끝나는 것이 프라이버시 설계의 핵심이다. 사용 기록은 밖으로 나가지 않는다.
- **점수·등급 매기기** — 자동 판정이 흔들리면 도구 전체 신뢰가 죽는다(daily.dev의 Clickbait Shield가 멀쩡한 제목을 오판하고 날마다 결과가 바뀌면서 겪은 일). 점수 대신 근거 문장을 쓴다.

## 다음

하나 남았다. 앞의 넷을 하면서 새로 생긴 것들은 `나중`으로 갔다.

### 1. 소스 추가 — 셋만

다이제스트가 4.5%(약 1,300토큰) 늘 뿐이다. 소스를 늘리는 것 자체가 목표가 아니므로 셋에서 멈춘다.

- **GitHub Releases Atom** (`/{owner}/{repo}/releases.atom`) — **유일하게 관련성 필터가 필요 없는 소스**다. 내가 의존하는 레포 목록을 내가 정하니 관련성이 100%고, LLM 선별을 건너뛰고 "지난 24시간" 날짜 컷으로 끝난다. 리포트의 `경고` 섹션을 먹여줄 주 재료다.
- **Lobsters** (`hottest.json`) — 무키·무인증, `tags[]`가 있어 HN보다 필터가 쉽다. 한 번에 25건(`?page=2`는 무시된다).
- **GeekNews** (`news.hada.io/rss/news`) — 한국어 제목이라 정보 밀도가 높다. HN에서 놓친 것을 한국어 맥락으로 재확인하는 용도.

버린 소스: arXiv(초록 865자 × 하루 830건, 제목이 학술 명명이라 관련성 판정이 안 된다 — cs.SE 35건만은 나중에 키워드 프리필터로 여지 있음), Reddit(무인증 수집 불안정), GitHub Trending(공식 API 없고 별 급증과 내 관심사의 상관이 낮다), Product Hunt(개발자 신호 희박).

## 한 것

`다음`에서 끝난 항목은 지우지 말고 여기로 내린다. 커밋 로그는 무엇을 바꿨는지만 말하고, 왜 그 선에서 멈췄는지는 말하지 않는다.

- **2026-08-20 — 누적 지식: 안 쓰인 후보를 버리지 않는다.** 하루 후보 25건 중 리포트에 오르는 12건 말고는 전부 사라졌다 — LLM이 이미 "프로젝트 X에 이런 이유로 관련 있다"고 판정한 것을 버리는 것이었다. `knowledge.py`가 주제별로 철하고(`채택`/`보류`), 보류는 다음 날 선별에 재심 대상으로 다시 올라간다. 보류가 나중에 채택되면 `log.md`에 `되살아남 N건`으로 남는다.
  형식은 [Karpathy `llm-wiki`](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) → [Google Cloud OKF v0.2](https://github.com/GoogleCloudPlatform/knowledge-catalog/tree/main/okf)를 따르되 **포맷만** 가져왔다. "LLM이 위키를 재편성한다"는 절반은 `선을 넘는 것`에 이미 있고 하루 2턴을 3턴으로 만든다 — 종합은 작문 턴에서 이미 끝나므로 철하기만 한다(0토큰). 토큰 절감 논거([84.6%](https://arxiv.org/abs/2604.11243))는 **우리에게 무효**다: 재질의를 안 하므로 선별 112K가 그대로 든다. 채택 이유는 못 하던 질의가 가능해지는 것뿐이다.
  **sqlite를 뺐다.** OKF가 DB를 요구하지 않고 우리 규모(연 수천 줄)에서 grep이 충분하다. 항목당 파일 하나도 안 만들었다 — Karpathy가 100개 소스 부근을 경고하는데 우리는 1주면 100개라, 페이지를 주제 단위로 묶어 파일 수를 프로젝트 수에 고정했다. YAML은 **쓰기만 준수**한다(`/usr/bin/python3`이 3.9.6이고 yaml이 없다).
  **본 URL 영구 제외는 버렸다.** 창이 겹치는 것은 용인하기로 했고, 필요한 절반("이미 리포트에 쓴 것은 재심에서 뺀다")은 채택/보류 구분이 공짜로 해결한다. `PROFILE.md`에 프로비넌스 필드를 붙이는 것도 뺐다 — 프로필은 지식 베이스가 아니라 느리게 변하는 것의 캐시다.
- **2026-08-20 — 집중도를 지수 감쇠로.** 총량과 마지막 활동일만 보던 것을 `Σ exp(-Δt/τ)`(τ=3일)로 바꿨다. 신호마다 최댓값을 1로 정규화한 뒤 가중치를 붙인다 — 프롬프트가 커밋보다 10~100배 많아 정규화 없이 더하면 커밋 신호가 사라진다. 세션 길이는 안 넣었다(디버깅 삽질이 길어 노이즈). **실측으로 갈렸다**: 프롬프트 211건인 프로젝트가 21건인 프로젝트보다 아래로 내려갔다 — 총량이 크지만 다 옛날 것이었다. 점수는 내보내지 않는다(`하지 않는 일`의 선). 정렬과 `최상위/상위/중간/약함` 표기까지만 쓴다. 1위의 5% 미만은 순위에서 빼는데, 이걸 넣자마자 **함정을 하나 밟았다** — 잘린 이름이 프로필에서 사라지고 그게 매일 `--check` 누락으로 잡혀 재생성이 무한히 트리거됐다. `profile.sh`가 `--list`를 커버리지 체크리스트로 함께 넘겨 막았다. 순위는 무엇을 올릴지만 정하고 무엇을 뺄지는 정하지 않는다.
- **2026-08-20 — 감지 신호 셋 추가(디스크 git·GitHub 이벤트·VS Code 창).** 에이전트 프롬프트는 관심을 말하지만 실제로 한 일은 말하지 않는다. 셋 다 `build_profile.py` 안에 넣었고(감지 축, 새 파일 없음) 다이제스트는 3.9%만 늘었다. 실제로 갈렸다 — 프롬프트는 많은데 내 커밋이 0건인 레포가 6개 나왔다. `/user/repos?sort=pushed`는 **버렸다**: 남이 푸시한 팀 레포가 최신으로 올라와 "내 활동만" 선을 넘는다. `active_projects()`는 일부러 안 건드렸다 — 그 함수가 프로필 재생성을 트리거하므로 LLM이 프로필에 안 쓸 이름이 섞이면 매일 재생성이 걸린다. 로드맵이 "VS Code는 최근성 순위가 없다"고 적어 뒀는데, `lastActiveWindow` 하나는 있다.
  **실제로 돌려 보고 프롬프트를 한 번 더 고쳤다.** 1차 결과에서 신호는 잘 붙었는데(로컬 프롬프트 19건뿐인 `wedding-planning`이 GitHub PR 8건·커밋 5건으로 집중 중에 승격 — 로컬 기록만으로는 안 보였을 프로젝트) 프로필이 프로젝트를 **떨어뜨려** 매칭 줄이 10 → 7로 줄었다. 2주 커밋 57건인 레포가 두 섹션 어디에도 없었다. 원인은 **신호가 '없는' 것을 '0'으로 읽은 것** — git 레포가 아니면 git 목록에 아예 안 나타나는데 그걸 활동 없음으로 봤다. `prompt-profile.md`에 (1) 강등은 신호가 있으면서 0일 때만, (2) 2주 활동이 있으면 하나도 빼지 말고 애매하면 방치로 넣어라 — 두 규칙을 넣어 10 → 18줄이 됐다. 프로필에 없는 프로젝트는 선별에서 후보조차 되지 못하므로 누락이 가장 비싼 실패다.
  `profile.sh`가 덮어쓰기 전에 `out/PROFILE-<날짜>.md`로 사본을 남긴다. 손으로 고친 부분 보존(`나중`)은 아직이지만, 최소한 재생성이 무엇을 바꿨는지는 diff로 본다.
  버린 신호: `~/.zsh_history`(cwd를 안 남기고 셸 종료 때만 기록), zoxide(DB 파일 부재), JetBrains(미사용), starred(관심사지 현재 작업이 아님), 커밋 검색 API(GitHub 이벤트가 상위집합).
- **2026-08-20 — 산출물을 `out/`으로 분리.** 하루 7개 파일이 루트에 쌓여 한 달이면 200개였다. 사이드카 파일명 규약은 그대로 두고 디렉토리만 옮겼다. `to_html.py`가 넘겨받은 리포트 경로의 디렉토리에서 형제를 찾으므로 **파이썬은 한 줄도 안 바뀌었고** `out/`이 등장하는 곳은 zsh 배선뿐이다. `.gitignore`도 7줄 패턴에서 `out/` 한 줄로 줄었다. 경로를 환경변수로 빼지는 않았다 — 위치를 바꿀 이유가 아직 없다.

## 나중

- 선정 기준 고정 섹션 — 무엇을 왜 고르는지 리포트에 상시 노출(Console.dev가 `selection-criteria` 페이지로 하는 것). 자동 판정의 불투명함을 상쇄한다.
- 프로필을 손으로 고친 부분을 재생성 때 보존 — 지금은 덮어쓴다. `PROFILE.md`를 직접 고치는 것이 가장 빠른 개선 수단이니, 그 수정이 살아남아야 한다.
- 리눅스 지원 — `launchd`·`osascript`·`open`을 걷어내야 한다. 수요가 생기면.
- `sqlite3` FTS5 파생 인덱스 — 누적 번들에 grep이 느려지는 것을 **측정한 뒤에**. 지금은 연 수천 줄이라 필요 없다.
- 재심 창(14일)과 보류 정리 정책 — 되살아남이 실제로 며칠 전 항목에서 나오는지 `log.md`를 몇 주 보고 정한다. 한 번도 안 되살아나는 항목이 쌓이면 그때 정리 규칙을 만든다.
- 집중도 상수 조정 — τ=3일, 신호 가중치 넷. 순위가 체감과 어긋나는 날이 반복되면 먼저 만질 곳이다.

## 검증되지 않은 가정

정직하게 적어 둔다.

- **일간이 맞는 주기인지.** TLDR이 일간으로 오픈율 41~47%를 내는 것이 근거지만, 그건 수신함·해지가 있는 이메일 뉴스레터다. 1인 로컬 HTML에서도 같은지는 몇 주 써봐야 안다.
- **12개 상한이 맞는 수인지.** TLDR의 10~15개에서 가져왔다. 프로젝트가 늘면 상한도 올려야 할지 모른다.
- **집중도 순위가 체감과 맞는지.** 지수 감쇠를 넣어 "많이 만진 것"과 "지금 집중하는 것"을 갈랐지만, τ=3일과 신호 가중치(에이전트 1.0 / 커밋 1.0 / GitHub 0.6 / 편집기 0.8)는 실측 한 번으로 정한 값이다. 순위가 체감과 어긋나는 날이 반복되면 먼저 만질 곳은 이 두 상수다.
