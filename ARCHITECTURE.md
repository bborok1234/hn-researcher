# 구조

## 이 도구가 하는 일

> 관심사를 묻지 않고 알아내서, 근거와 함께 오늘 할 일로 바꿔 주는 필터.

세 축이고, 각 축이 파일로 나뉜다.

| 축 | 하는 일 | 담당 |
|---|---|---|
| 감지 | 내가 지금 붙어 있는 일을 알아낸다 | `build_profile.py`, `profile.sh` |
| 선별 | 그에 맞는 양질의 정보를 골라낸다 | `fetch_hn.py`, `prompt-select.md`, `fetch_pages.py` |
| 정리 | 읽고 파악하고 바로 쓰게 만든다 | `prompt-write.md`, `to_html.py` |

## 파이프라인

`daily.sh`가 하루 한 번 `run.sh`를 부르고, `run.sh`가 세 단계를 돈다.

| 단계 | 하는 일 | 도구 | 비용 |
|---|---|---|---|
| 1 선별 | 하루치 목록 전체를 한 번 읽고 후보 20~25건을 고른다 | 없음, 1턴 | ~112K 토큰 |
| 2 수집 | 원문·HN 댓글을 받아 텍스트로 정리, 읽기시간 계산 | 없음 (순수 코드) | **0** |
| 3 작문 | 정리된 본문만 받아 리포트를 쓴다 | 없음, 1턴 | ~110K 토큰 |

**왜 셋으로 나누는가.** `claude -p`는 에이전트 루프라서 **매 턴마다 대화 전체를 다시 보낸다.** 한 세션에서 선별·페치·작문을 다 하던 초기 구현은 20턴을 돌며 읽어들인 웹페이지를 계속 누적해 **회당 550만 토큰**을 썼다. 그중 57%가 누적된 페이지였다. 페치를 LLM에서 코드로 옮기고 도구 호출을 0으로 만들자 **회당 22만 토큰, 25배**가 됐다.

세 레버가 각각 기여했다.
1. **페치를 코드로** — 컨텍스트 누적이 사라진다. 가장 큰 몫.
2. **선별 목록에서 URL 제거** — URL은 토큰 밀도가 높아 1,100줄에 두 개씩 붙이면 다이제스트가 21만 토큰까지 부푼다. 216KB → 85KB. 실제 URL은 `--urls`로 ID→URL 맵에 따로 저장해 2단계에서 되찾는다.
3. **`--safe-mode`** — 스킬·플러그인 정의를 시스템 프롬프트에서 뺀다. "OK" 한 마디에도 들던 고정비가 턴당 50K → 29K.

## 데이터 흐름

날짜를 키로 하는 사이드카 파일 규약을 쓴다. 같은 디렉토리에 `<이름>-<YYYY-MM-DD>.<확장자>`로 놓이고, 뒤 단계가 앞 단계의 산출물을 파일명으로 찾는다.

**산출물은 전부 `out/` 아래에 쌓인다.** 코드와 섞이면 한 달에 200개가 루트에 깔려 저장소가 읽히지 않는다. 규약은 디렉토리만 바뀐 것이고, `to_html.py`는 넘겨받은 리포트 경로의 디렉토리에서 형제를 찾으므로 배선에 `out/`이 등장하는 곳은 zsh 스크립트뿐이다.

```
(경로는 모두 out/ 아래)
fetch_hn.py ──→ digest-DATE.md        선별용 목록 (제목 + #ID, URL 없음)
            └─→ urls-DATE.json        {게시물ID: URL}
build_profile.py --list ─→ projects-DATE.txt   추적 중인 프로젝트 목록
1단계 ──────→ candidates-DATE.json    고른 후보 (id/title/source/project/why)
fetch_pages.py ─→ pages-DATE.md       본문 + 읽기시간 + 원문·댓글 구분
3단계 ──────→ report-DATE.md          리포트 본문
to_html.py ──→ report-DATE.html       뉴스레터
```

`to_html.py`는 리포트 파일명에서 날짜를 뽑아 옆의 `digest`·`candidates`·`pages`·`projects`를 찾는다. 없으면 해당 표시만 생략하고 계속 렌더한다. 배선을 늘리지 않으려는 선택이고, 대신 파일명 규약을 깨면 조용히 기능이 빠진다.

## 개인화가 만들어지는 방식

`build_profile.py`가 다섯 곳을 읽는다. 앞의 둘이 **관심**을, 뒤의 셋이 **실제로 한 일**을 말한다.

- `~/.claude/history.jsonl` — 프롬프트 본문 + 프로젝트 경로 + 시각
- `~/.codex/sessions/**/*.jsonl` — rollout 첫 줄 `session_meta`의 cwd·시각·서브에이전트 여부
- **디스크 git** — 에이전트 기록에 나온 경로에만 `git log`를 돌린다. 홈 전체를 걷지 않는 이유는 새 레포 발견을 GitHub 이벤트가 이미 담당하기 때문이다. 커밋 수는 `--author`로 내 것만 센다 — 팀 레포에서 전체를 세면 남의 활동이 섞인다. **커밋 0건이면 그 레포는 작업이 아니라 조사였다.** 브랜치명(`feat/time-travel-emulator`)이 지금 붙어 있는 주제를 직접 말해준다
- **GitHub 이벤트** — `gh api /users/{me}/events`, 100건×3페이지(4페이지는 422). 로컬에 안 남는 것을 준다: 내가 올린 PR·리뷰·코멘트, private·조직 레포 포함
- **VS Code `windowsState`** — 열려 있는 창. '지금 이 순간' 신호로 가장 순수하다. 순위는 없고 열림/닫힘뿐이지만 `lastActiveWindow` 하나는 최근성을 준다

`/user/repos?sort=pushed`는 **일부러 쓰지 않는다.** 남이 푸시한 팀 레포가 최신으로 올라와 "내 활동만" 선을 넘는다. 이벤트는 actor가 나인 것만 온다.

셋 다 없어도 파이프라인은 돌아야 한다 — `gh` 미설치·미인증, git 아닌 경로, VS Code 미설치는 모두 해당 섹션만 비우고 넘어간다.

**새 신호는 다이제스트만 살찌우고 `active_projects()`에는 넣지 않았다.** 그 함수의 결과가 `--check`로 프로필 재생성을 트리거하므로, LLM이 프로필에 안 쓸 만한 이름(웹에서 커밋만 한 레포 등)이 섞이면 매일 재생성이 걸린다. 임시 폴더로 이미 밟은 함정과 같은 모양이다.

### 집중도는 총량이 아니라 감쇠로 잰다

다섯 신호를 프로젝트별로 합쳐 **빈도 × 지수적 최근성 감쇠**(`Σ exp(-Δt/τ)`, τ=3일)로 순위를 낸다. Mylyn의 degree-of-interest 모델(Kersten & Murphy)이 쓰는 형태고, **누적 총량은 쓰지 않는다.** 총량으로 재면 "2주 전에 몰아서 한 것"과 "어제부터 붙어 있는 것"이 같은 값이 나온다 — 실측으로 프롬프트 211건짜리 프로젝트가 21건짜리보다 아래로 내려갔다.

- **신호마다 최댓값을 1로 정규화한 뒤 가중치를 붙인다.** 프롬프트 건수가 커밋 건수보다 10~100배 많아, 정규화 없이 더하면 커밋 신호가 사라진다. 절대값 비교는 애초에 의미가 없다(사람마다 커밋 입도가 다르다) — **같은 사용자 안에서 레포 간 상대 비교로만** 쓴다.
- **세션 길이는 넣지 않는다.** 디버깅 삽질이 길어서 노이즈다.
- **1위의 5% 미만은 순위에서 뺀다.** 감쇠를 걸면 "2주 전에 한 번 만진 것"이 꼬리로 길게 남는다(실측 25개 중 14개). 절대값이 아니라 비율이라 활동이 적은 사용자에게는 자동으로 느슨해진다.
- **점수를 내보내지 않는다.** 정렬과 `최상위/상위/중간/약함` 표기까지만 쓴다. 자동 판정 숫자가 날마다 흔들리면 도구 전체 신뢰가 죽는다 — `ROADMAP.md`의 `하지 않는 일`에 있는 선이다.
- **순위는 무엇을 위로 올릴지만 정한다.** 무엇을 뺄지는 정하지 않는다. `profile.sh`가 `--list`를 커버리지 체크리스트로 함께 넘기지 않으면, 순위에서 잘린 이름이 프로필에서 사라지고 그것이 매일 `--check` 누락으로 잡혀 재생성이 무한히 트리거된다. 실제로 한 번 밟았다.

이걸 프로젝트별로 집계해 다이제스트를 만들고, `profile.sh`가 LLM에 넘겨 `PROFILE.md`를 쓴다. 프로필에는 프로젝트마다 `매칭:` 키워드 줄이 붙는데, **이 줄이 1,100여 건 목록에서 관련 글을 낚는 고리**다. 추천이 안 맞을 때 가장 빠른 개선은 이 줄에 단어를 더하는 것이다.

세 가지 모드가 있다.

- 인자 없음 — 활동 다이제스트를 stdout으로
- `--list` — 최근 14일 활동 프로젝트 목록 (리포트 프롬프트의 커버리지 체크리스트, 화면의 로스터 칩)
- `--check <파일...>` — 그 문서들에 없는 활동 프로젝트를 출력하고 있으면 exit 1. `daily.sh`가 매일 이걸로 새 프로젝트를 감지해 프로필을 즉시 갱신한다

## 리포트 구조가 왜 이런가

**본문은 프로젝트가 아니라 '결정 종류'로 나뉜다.** 상단 로스터 칩이 이미 프로젝트 축을 담당하므로, 본문이 같은 축을 또 쓰면 정보가 겹친다. 섹션 순서는 시급성 감쇠다 — 경고(지금) → 적용(오늘) → 실험(이번 주) → 수익(2주) → 만들 것(주말).

**섹션은 내용이 있을 때만 나타난다.** 다섯 개를 매일 채우게 하면 패딩이 들어온다. 이 카테고리에서 사용자가 이탈하는 1번 원인은 정보 부족이 아니라 미독 누적이다.

**패딩을 실제로 막는 건 섹션 유무가 아니라 항목별 통과 조건이다.**

- 수익 — 누가 돈을 내는지 한 문장. 못 쓰면 '만들 것'으로 강등
- 만들 것 — 프로필의 어떤 근거로 취향이라 판단했는지 인용. 없으면 버림
- 전 항목 — 실행 한 줄("무엇을 어디에 어떻게"). 동기 자료만 준 군의 실행률이 35~39%인데 언제·어디서·어떻게를 한 문장 적게 한 군은 91%였다
- 전체 12개 상한

**소싱을 앞세우고 AI를 앞세우지 않는다.** AI 답변의 최대 실패 모드가 소싱(출처 누락·오귀속) 31%이고, "AI 생성" 라벨은 정확한 글에도 신뢰 페널티를 준다. 그래서 항목마다 원문 링크·읽기시간·원문/댓글 구분을 붙이고 머리말에 "N건 읽고 M건 추려 K건 확인"을 쓴다. 읽기시간은 **모델이 추측하면 틀리므로** `fetch_pages.py`가 글자수로 계산한다.

## 스케줄

`launchd` 하나로만 돈다(`com.<사용자명>.hn-researcher`). `install.sh`가 plist를 만들고 등록한다.

**cron을 쓰면 안 된다.** GUI(Aqua) 세션 밖이라 로그인 키체인의 OAuth 토큰에 접근하지 못해 `claude` CLI가 항상 `Failed to authenticate: OAuth session expired`로 죽는다. 단독 실행으로 확인한 사항이다.

`daily.sh`는 두 겹으로 중복을 막는다. `mkdir logs/.lock`이 원자적 잠금이고(리포트 완성까지 몇 분 걸려 파일 존재 확인만으로는 경합을 못 막는다), 2시간 넘은 잠금은 죽은 프로세스가 남긴 것으로 보고 회수한다. 그 뒤 오늘 리포트가 이미 있으면 즉시 종료한다. 호출 흔적은 `logs/runs.log`에 남아서, 아침에 안 돌았을 때 추측 없이 판단할 수 있다.

## 스택

확정된 목록이다. 여기에 없는 것을 추가하려면 먼저 물어볼 것.

| 층 | 결정 | 이유 |
|---|---|---|
| 로직 | **python3 (시스템 파이썬)** | macOS 기본 탑재. 설치 단계가 없다 |
| 의존성 | **표준 라이브러리만, 0개** | `urllib`·`json`·`html.parser`·`concurrent.futures`·`sqlite3`로 예정 기능까지 전부 된다. 클론 후 즉시 실행이 깨지지 않는다 |
| 배선 | **zsh** | 진입점과 launchd 접착제만. 로직은 넣지 않는다 |
| LLM 호출 | **`claude -p` CLI, 구독 인증** | 구독료를 이미 낸다. Messages API는 옵션으로만 두고 기본 경로로 삼지 않는다 |
| 모델 | 일일 `sonnet` / 주간 상위 모델 | 호출마다 `--model` 명시. 생략하면 대화용 기본값을 물려받아 사용량이 몇 배가 된다 |
| 스케줄 | **launchd 단독** | cron은 GUI 세션 밖이라 키체인에 못 닿아 인증이 항상 실패한다 |
| 저장 | **파일시스템, 날짜 키 사이드카** | grep이 되는 것이 1인 도구에서 가장 빠른 조회다 |
| 누적 지식 | markdown 정본 + `sqlite3` 파생 인덱스 (예정) | 인덱스는 언제든 버리고 재생성 가능해야 한다 |
| 렌더 | **자체 변환기(`to_html.py`)** | 리포트가 쓰는 문법만 지원하면 40줄이면 된다 |
| 스타일 | 인라인 CSS, 시스템 폰트 | 외부 요청 0. 오프라인에서도 열린다 |
| 테스트 | **stdlib `unittest`** | 프레임워크를 얹을 규모가 아니다 |
| 외부 서비스 | HN Algolia API (무키) | 예정 추가분(Lobsters·GitHub Releases Atom·GeekNews)도 전부 무키 |
| GitHub 신호 | `gh` CLI 경유 | 이미 인증돼 있고 파이썬 의존성이 늘지 않는다 |
| 플랫폼 | **macOS 전용** | `launchd`·`osascript`·`open`에 묶여 있다 |

**쓰지 않기로 한 것**: uv·pyproject(클론 후 즉시 실행이 깨진다), pytest, 서드파티 HTTP·HTML·템플릿 라이브러리, pandoc, 벡터스토어, 그래프DB, 서버, Docker.

서버가 없다. 사용 기록은 로컬에서만 읽고, 외부로 나가는 것은 리포트 생성 프롬프트뿐이다.

## 반복해서 밟은 함정

다시 밟지 않기 위한 목록이다.

**Codex 서브에이전트 세션을 빼면 안 된다.** Codex Desktop으로 주력 작업을 하면 세션의 99%가 서브에이전트다(한 프로젝트가 7일간 187건 중 186건). 이걸 건너뛰면 **가장 열심히 하는 프로젝트일수록 내용이 통째로 비어** 프로필에서 과소평가된다. 대신 서브에이전트는 부모의 첫 메시지를 물려받아 같은 문장이 수십 번 반복되므로 중복 제거와 Codex 내부 보일러플레이트 필터가 함께 필요하다.

**`~/.codex/session_index.jsonl`은 이름 붙은 스레드만 담는다.** 반드시 `sessions/` 원장을 읽어야 한다.

**`--allowedTools ""`는 무시된다.** 도구를 껐다고 생각했는데 모델이 `Bash("echo done")`을 실제로 실행해 237K 토큰짜리 턴을 날렸다. `--disallowedTools`로 명시적으로 거부해야 한다.

**`--disallowedTools`는 가변 인자다.** 뒤따르는 인자를 도구 이름으로 삼켜서 프롬프트가 사라진다. 반드시 쉼표로 구분하고 프롬프트를 이 옵션보다 앞에 둔다.

**`claude -p`는 프롬프트를 인자로 줘도 stdin을 3초 기다린다.** `</dev/null`을 붙인다.

**모델을 명시하지 않으면 `~/.claude/settings.json`의 대화용 기본값을 물려받는다.** 대화창에서 `/model`을 올리면 다음 날 아침 배치가 조용히 최상위 티어로 돌며 사용량이 몇 배가 된다.

**`[ 조건 ] && 명령`을 스크립트 마지막 줄에 두지 마라.** 조건이 거짓이면 스크립트 종료 코드가 1이 되어 `launchctl`이 실패로 기록한다. 중간에서는 무해하다.

**리포트의 섹션 제목 표기가 흔들린다.** 같은 프롬프트인데도 `## 제목`으로 쓸 때와 `**제목**` 한 줄로 쓸 때가 있다. `to_html.py`의 `tokenize()`가 둘을 흡수하고, 볼드 한 줄이 소문자 슬러그면 프로젝트 이름, 한국어면 섹션 제목으로 가른다.

**집중도 순위로 프로필에서 프로젝트를 빼면 안 된다.** 순위 하위를 잘라내자 그 이름이 매일 `--check` 누락으로 잡혀 재생성이 무한히 트리거됐다. `active_projects()`가 통과시키는 목록과 프로필에 실리는 목록은 반드시 같아야 한다 — `profile.sh`가 `--list`를 프롬프트에 함께 넣는 이유다.

**`~/Documents/Codex/<날짜>/`는 임시 작업 폴더다.** 프로젝트로 집계하면 프로필에 실릴 리 없는 이름이 매일 누락으로 잡혀 재생성이 무한히 트리거된다.

## 코드 아키텍처

파일 1,000줄 규모다. 레이어를 얹지 않는다. 규칙 넷만 지킨다.

**1. 파일 안에서 2단을 유지한다.** 순수 함수를 위에, I/O는 `if __name__` 아래. 네 파일 모두 이미 이 형태다(`to_markdown`·`url_map`·`html_to_text`·`active_projects`가 순수, 네트워크와 파일 접근은 진입점). 읽는 순서가 데이터 흐름과 같아지는 것이 이 배치의 유일한 이득이므로, **`core/`·`io/` 디렉토리로 물리 분리하면 그 이득이 사라진다.**

**2. LLM 호출은 `gen_report.sh` 하나만.** 재시도·출력 검증·도구 차단·stdin 처리가 전부 거기 있다. CLI 함정 넷이 모두 이 호출 형태에 걸리므로, 호출 지점이 흩어지면 반드시 한쪽만 고치게 된다. (`install.sh`의 인증 확인 1회는 파이프라인이 아니라 설치 절차다.)

**3. 단계 간 계약은 "한 줄 형식 + dict"다.** `dataclass`·`TypedDict`는 런타임 검증을 하지 않으므로 JSON 경계에서 이름만 붙는 층이 된다. 실제 계약은 다이제스트 한 줄 형식과 `urls-DATE.json`이고, 검증은 진입점 `assert` 하나와 `.get()` 기본값, 그리고 사이드카가 없으면 해당 표시만 생략하는 정책으로 충분하다.

**4. 파일은 축이 늘 때만 추가한다.** 줄 수로 쪼개지 않는다 — 표준 라이브러리도 `argparse.py` 2,650줄, `tarfile.py` 2,933줄이 단일 모듈이고, 과분해는 얕은 모듈과 층 넘는 수정 비용을 만든다. 이대로 했다 — git 스캔·GitHub 이벤트·편집기 창은 셋 다 `build_profile.py` 안이다(감지 축, 199 → 368줄). 지수 감쇠도 `build_profile.py`의 순수 함수로 들어갔다(`decay`·`_relative`·`focus`). 남은 배치: 소스 3개 추가 → `sources.py` 하나(새 축), URL 정규화·다이제스트 줄 포맷 → 기존 파일의 순수 함수.

**하지 않는 것**: `dataclass`/`TypedDict` 계약 층, pydantic류 런타임 검증, `Source` 추상 클래스·포트-어댑터, `core/`·`io/` 디렉토리 분리, 줄 수 기반 분할 규칙, 단계 간 중간 스키마 파일.

## 확장 지점

**정보 소스를 추가하려면** `fetch_hn.py`가 내놓는 다이제스트 한 줄 형식(`- [점수p/댓글c] #ID 제목`)에 맞춰 줄을 더하고 `urls-DATE.json`에 ID→URL을 넣으면 된다. 뒤 단계는 소스를 구분하지 않는다.

**프롬프트를 고치는 것이 코드를 고치는 것보다 먼저다.** 선별 기준·리포트 구조·통과 조건은 전부 `prompt-*.md`에 있고, 파이프라인 코드는 그것을 옮기는 배관일 뿐이다.
